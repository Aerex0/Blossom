apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: kill-collab-pod
  labels:
    app: google-docs
    component: chaos
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: google-docs
      component: collab
  gracePeriod: 0
